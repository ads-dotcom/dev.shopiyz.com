openapi: 3.1.0
info:
  title: Shopiyz Storefront API
  version: "2026-07-21"
  description: |-
    Versioned, read-optimized storefront contract used by Shopiyz themes and headless clients.
    First-party same-origin themes may read public endpoints without a token. Cross-origin clients use a storefront token with storefront:read.
servers:
  - url: https://{store}.myshopiyz.com/api/storefront/v1
    variables:
      store:
        default: development
        description: Store subdomain or custom store hostname.
x-shopiyz-base-path: "/api/storefront/v1"
x-shopiyz-generated-at: "2026-07-21T12:02:49.150Z"
paths:
  /health:
    get:
      operationId: getStorefrontHealth
      summary: "Storefront API health"
      description: "Dağıtılan site Worker'ında Storefront API v1 yönlendirmesinin hazır olduğunu doğrular."
      tags: ["Runtime"]
      x-shopiyz-auth: public
      x-shopiyz-cache: none
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /page:
    get:
      operationId: getStorefrontPage
      summary: "Get page runtime payload"
      description: "Mağaza, yayınlanmış tema, sayfa düzenleri, gerekli katalog verileri, menüler ve sayfaya özel kaynağı tek payload içinde döndürür."
      tags: ["Runtime"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: pageType
          in: query
          required: false
          description: "home, product, collection, collection-list, search, page, blog-category veya blog-post."
          schema: { type: string }
        - name: path
          in: query
          required: false
          description: "Tarayıcıdaki kanonik storefront yolu."
          schema: { type: string }
        - name: product
          in: query
          required: false
          description: "Ürün handle veya kimliği."
          schema: { type: string }
        - name: collection
          in: query
          required: false
          description: "Koleksiyon handle veya kimliği."
          schema: { type: string }
        - name: blog
          in: query
          required: false
          description: "Blog handle."
          schema: { type: string }
        - name: blogPost
          in: query
          required: false
          description: "Blog yazısı handle."
          schema: { type: string }
        - name: page
          in: query
          required: false
          description: "İçerik sayfası handle."
          schema: { type: string }
        - name: policy
          in: query
          required: false
          description: "Politika handle."
          schema: { type: string }
        - name: limit
          in: query
          required: false
          description: "Sayfa başına sonuç; en fazla 48."
          schema: { type: integer, minimum: 1, maximum: 48 }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontPageResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /shop:
    get:
      operationId: getStorefrontShop
      summary: "Get shop and theme shell"
      description: "Mağaza kimliği, herkese açık ayarlar, aktif tema ve header/footer düzenlerini döndürür."
      tags: ["Runtime"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShopResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /localization:
    get:
      operationId: getStorefrontLocalization
      summary: "Get locale, currency and markets"
      description: "Tema fiyat formatı için mağaza para birimini, locale bilgisini, ondalık basamakları ve aktif pazar ülkelerini döndürür."
      tags: ["Runtime"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocalizationResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /products:
    get:
      operationId: listStorefrontProducts
      summary: "List and filter published products"
      description: "Aktif ürünleri fiyat, marka, kategori, ürün tipi, etiket, renk, beden ve stok facetleri; sıralama ve kararlı sayfalama bilgileriyle listeler."
      tags: ["Catalog"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: page
          in: query
          required: false
          description: "Birden başlayan sonuç sayfası."
          schema: { type: integer, minimum: 1 }
        - name: limit
          in: query
          required: false
          description: "Sayfa başına sonuç; en fazla 48."
          schema: { type: integer, minimum: 1, maximum: 48 }
        - name: sort
          in: query
          required: false
          description: "Katalog sıralaması."
          schema: { type: string, enum: ["default", "title-asc", "title-desc", "newest", "oldest", "price-asc", "price-desc"] }
        - name: vendor
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan marka değerleri."
          schema: { type: string }
        - name: productType
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan ürün tipi değerleri."
          schema: { type: string }
        - name: category
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan kategori değerleri."
          schema: { type: string }
        - name: tag
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan etiket değerleri."
          schema: { type: string }
        - name: color
          in: query
          required: false
          description: "Varyant renk seçeneği; Color, Colour, Renk, Farbe ve Couleur adları tanınır."
          schema: { type: string }
        - name: size
          in: query
          required: false
          description: "Varyant beden seçeneği; Size, Beden, Größe ve Taille adları tanınır."
          schema: { type: string }
        - name: availability
          in: query
          required: false
          description: "Satılabilirlik filtresi."
          schema: { type: string, enum: ["in-stock", "out-of-stock", "preorder"] }
        - name: priceMin
          in: query
          required: false
          description: "Mağaza para biriminde minimum fiyat."
          schema: { type: number, minimum: 0 }
        - name: priceMax
          in: query
          required: false
          description: "Mağaza para biriminde maksimum fiyat."
          schema: { type: number, minimum: 0 }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductConnectionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /products/{handle}:
    get:
      operationId: getStorefrontProduct
      summary: "Get published product"
      description: "Ürün handle veya kimliğiyle ürün detayını döndürür; taslak ürünleri yalnızca yetkili preview oturumu görebilir."
      tags: ["Catalog"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: handle
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /collections:
    get:
      operationId: listStorefrontCollections
      summary: "List published collections"
      description: "Yayınlanmış koleksiyonları ve Storefront kart alanlarını listeler."
      tags: ["Catalog"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionListResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /collections/{handle}:
    get:
      operationId: getStorefrontCollection
      summary: "Get collection, products and facets"
      description: "Koleksiyon bilgisiyle birlikte filtrelenmiş ürünleri, facet sayılarını, aktif filtreleri, sıralamayı ve sayfalama meta verisini döndürür."
      tags: ["Catalog"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: handle
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: page
          in: query
          required: false
          description: "Birden başlayan sonuç sayfası."
          schema: { type: integer, minimum: 1 }
        - name: limit
          in: query
          required: false
          description: "Sayfa başına sonuç; en fazla 48."
          schema: { type: integer, minimum: 1, maximum: 48 }
        - name: sort
          in: query
          required: false
          description: "Katalog sıralaması."
          schema: { type: string, enum: ["default", "title-asc", "title-desc", "newest", "oldest", "price-asc", "price-desc"] }
        - name: vendor
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan marka değerleri."
          schema: { type: string }
        - name: productType
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan ürün tipi değerleri."
          schema: { type: string }
        - name: category
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan kategori değerleri."
          schema: { type: string }
        - name: tag
          in: query
          required: false
          description: "Virgülle ayrılmış veya tekrarlanan etiket değerleri."
          schema: { type: string }
        - name: color
          in: query
          required: false
          description: "Varyant renk seçeneği; Color, Colour, Renk, Farbe ve Couleur adları tanınır."
          schema: { type: string }
        - name: size
          in: query
          required: false
          description: "Varyant beden seçeneği; Size, Beden, Größe ve Taille adları tanınır."
          schema: { type: string }
        - name: availability
          in: query
          required: false
          description: "Satılabilirlik filtresi."
          schema: { type: string, enum: ["in-stock", "out-of-stock", "preorder"] }
        - name: priceMin
          in: query
          required: false
          description: "Mağaza para biriminde minimum fiyat."
          schema: { type: number, minimum: 0 }
        - name: priceMax
          in: query
          required: false
          description: "Mağaza para biriminde maksimum fiyat."
          schema: { type: number, minimum: 0 }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /menus:
    get:
      operationId: listStorefrontMenus
      summary: "List storefront menus"
      description: "Header, footer, müşteri hesabı ve özel konumlardaki çok seviyeli menü ağacını; güvenli URL ve yeni sekme davranışıyla döndürür."
      tags: ["Catalog"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MenuListResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /image:
    get:
      operationId: getStorefrontImage
      summary: "Proxy a storefront image"
      description: "İzin verilen medya kaynağını Storefront için güvenli response başlıklarıyla iletir."
      tags: ["Catalog"]
      x-shopiyz-auth: public
      x-shopiyz-cache: edge
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: source
          in: query
          required: false
          description: "İzin verilen kaynak görsel URL'si."
          schema: { type: string }
        - name: width
          in: query
          required: false
          description: "Dönüştürülen görsel genişliği."
          schema: { type: integer, minimum: 1 }
        - name: quality
          in: query
          required: false
          description: "Görsel kalite yüzdesi."
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: format
          in: query
          required: false
          description: "Çıktı formatı."
          schema: { type: string, enum: ["auto", "avif", "webp", "jpeg", "png"] }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /pages/{handle}:
    get:
      operationId: getStorefrontContentPage
      summary: "Get published content page"
      description: "Yayınlanmış kurumsal, iletişim, hakkımızda, SSS veya özel sayfayı handle ile döndürür."
      tags: ["Content"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: handle
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentPageResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /blogs:
    get:
      operationId: listStorefrontBlogs
      summary: "List published blogs"
      description: "Yayınlanmış blog kategorilerini ve tema kart alanlarını listeler."
      tags: ["Content"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlogListResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /blogs/{handle}:
    get:
      operationId: getStorefrontBlog
      summary: "Get blog and posts"
      description: "Blog bilgisi ile yayınlanmış yazı listesini döndürür."
      tags: ["Content"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: handle
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlogResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /blogs/{blogHandle}/{postHandle}:
    get:
      operationId: getStorefrontBlogPost
      summary: "Get blog post"
      description: "Blog yazısını, blog bağlamını ve yazıya atanmış ürünleri döndürür."
      tags: ["Content"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: blogHandle
          in: path
          required: true
          schema: { type: string }
        - name: postHandle
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlogPostResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /promotions:
    get:
      operationId: listStorefrontPromotions
      summary: "List public automatic promotions"
      description: "İndirim kodlarını veya özel müşteri kurallarını açmadan aktif otomatik ürün, sipariş, X al Y kazan ve ücretsiz kargo promosyonlarını döndürür."
      tags: ["Merchandising"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionListResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /search:
    get:
      operationId: searchStorefront
      summary: "Search storefront"
      description: "Ürün, koleksiyon, sayfa ve blog sonuçlarını filtre ve sıralama seçenekleriyle döndürür."
      tags: ["Search and discovery"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: edge
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: q
          in: query
          required: false
          description: "Arama metni."
          schema: { type: string }
        - name: searchPage
          in: query
          required: false
          description: "Arama sonuç sayfası."
          schema: { type: integer, minimum: 1 }
        - name: limit
          in: query
          required: false
          description: "Sayfa başına sonuç; en fazla 48."
          schema: { type: integer, minimum: 1, maximum: 48 }
        - name: searchSort
          in: query
          required: false
          description: "Arama sıralaması."
          schema: { type: string, enum: ["relevance", "title", "newest", "price-asc", "price-desc"] }
        - name: searchType
          in: query
          required: false
          description: "Arama sonuç türü."
          schema: { type: string, enum: ["all", "products", "collections", "pages", "blog_posts"] }
        - name: searchVendor
          in: query
          required: false
          description: "Arama marka filtresi."
          schema: { type: string }
        - name: searchCategory
          in: query
          required: false
          description: "Arama kategori filtresi."
          schema: { type: string }
        - name: searchProductType
          in: query
          required: false
          description: "Arama ürün tipi filtresi."
          schema: { type: string }
        - name: searchColor
          in: query
          required: false
          description: "Arama renk filtresi."
          schema: { type: string }
        - name: searchSize
          in: query
          required: false
          description: "Arama beden filtresi."
          schema: { type: string }
        - name: searchAvailability
          in: query
          required: false
          description: "Yalnız stokta satılabilir ürünler."
          schema: { type: boolean }
        - name: searchPriceMax
          in: query
          required: false
          description: "Arama maksimum fiyatı."
          schema: { type: number, minimum: 0 }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /smart-search:
    get:
      operationId: getSmartSearch
      summary: "Get predictive search results"
      description: "Arama önerileri ve yapılandırılmış akıllı arama sonuçlarını döndürür."
      tags: ["Search and discovery"]
      x-shopiyz-auth: public
      x-shopiyz-cache: private
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: q
          in: query
          required: false
          description: "Arama metni."
          schema: { type: string }
        - name: limit
          in: query
          required: false
          description: "Sayfa başına sonuç; en fazla 48."
          schema: { type: integer, minimum: 1, maximum: 48 }
        - name: sessionId
          in: query
          required: false
          description: "Anonim arama oturumu."
          schema: { type: string }
        - name: deviceType
          in: query
          required: false
          description: "İstemci cihaz sınıfı."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /smart-search-click:
    post:
      operationId: trackSmartSearchClick
      summary: "Track a search click"
      description: "Arama sonucuna tıklama sinyalini anonim oturum kapsamında kaydeder."
      tags: ["Search and discovery"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /quick-view-product:
    get:
      operationId: getQuickViewProduct
      summary: "Get quick-view products"
      description: "Hızlı görünüm ve son görüntülenenler bileşenleri için bir veya daha fazla ürün döndürür."
      tags: ["Search and discovery"]
      x-shopiyz-auth: public
      x-shopiyz-cache: edge
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: product
          in: query
          required: false
          description: "Ürün handle veya kimliği."
          schema: { type: string }
        - name: products
          in: query
          required: false
          description: "Virgülle ayrılmış ürün kimlikleri."
          schema: { type: string }
        - name: revision
          in: query
          required: false
          description: "Storefront cache revision."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /product-viewers:
    get:
      operationId: getProductViewers
      summary: "Get product viewer count"
      description: "Ürün için anonim ve yaklaşık aktif görüntüleyici sayısını döndürür."
      tags: ["Search and discovery"]
      x-shopiyz-auth: public
      x-shopiyz-cache: private
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: productId
          in: query
          required: false
          description: "Ürün kimliği."
          schema: { type: string }
        - name: productHandle
          in: query
          required: false
          description: "Ürün handle."
          schema: { type: string }
        - name: windowSeconds
          in: query
          required: false
          description: "Aktif izleyici zaman penceresi."
          schema: { type: integer, minimum: 30 }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
    post:
      operationId: recordProductViewer
      summary: "Record product viewer"
      description: "Ürün görüntüleme oturumunu günceller."
      tags: ["Search and discovery"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: productId
          in: query
          required: false
          description: "Ürün kimliği."
          schema: { type: string }
        - name: productHandle
          in: query
          required: false
          description: "Ürün handle."
          schema: { type: string }
        - name: windowSeconds
          in: query
          required: false
          description: "Aktif izleyici zaman penceresi."
          schema: { type: integer, minimum: 30 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /customer:
    get:
      operationId: getStorefrontCustomer
      summary: "Get customer session"
      description: "Mevcut müşteri oturumunu ve hesap özetini döndürür."
      tags: ["Customer and access"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: private
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
    post:
      operationId: mutateStorefrontCustomer
      summary: "Run customer action"
      description: "Giriş, kayıt, çıkış, profil ve bülten gibi müşteri eylemlerini çalıştırır."
      tags: ["Customer and access"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /customer/oauth/authorize:
    get:
      operationId: authorizeStorefrontCustomer
      summary: "Authorize external customer client"
      description: "OAuth Authorization Code + PKCE S256 akışını başlatır; state, nonce ve tam redirect URI eşleşmesi zorunludur."
      tags: ["Customer and access"]
      x-shopiyz-auth: public
      x-shopiyz-cache: none
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: response_type
          in: query
          required: false
          description: "OAuth Authorization Code akışı."
          schema: { type: string, enum: ["code"] }
        - name: client_id
          in: query
          required: false
          description: "Admin panelindeki Storefront token kaydının client ID değeri."
          schema: { type: string }
        - name: redirect_uri
          in: query
          required: false
          description: "İstemciye kayıtlı adresle tam eşleşen HTTPS callback URI."
          schema: { type: string }
        - name: scope
          in: query
          required: false
          description: "Boşlukla ayrılmış customer:read ve customer:write kapsamları."
          schema: { type: string }
        - name: state
          in: query
          required: false
          description: "CSRF koruması için en az 16 karakterlik tek kullanımlık istemci state değeri."
          schema: { type: string }
        - name: nonce
          in: query
          required: false
          description: "Replay koruması için en az 16 karakterlik tek kullanımlık nonce."
          schema: { type: string }
        - name: code_challenge
          in: query
          required: false
          description: "PKCE S256 code challenge."
          schema: { type: string }
        - name: code_challenge_method
          in: query
          required: false
          description: "Yalnız S256 desteklenir."
          schema: { type: string, enum: ["S256"] }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /customer/oauth/token:
    post:
      operationId: exchangeStorefrontCustomerToken
      summary: "Exchange or refresh customer token"
      description: "Tek kullanımlık yetkilendirme kodunu veya dönen refresh tokeni kısa ömürlü access token ile değiştirir."
      tags: ["Customer and access"]
      x-shopiyz-auth: public
      x-shopiyz-cache: none
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OAuthTokenRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/OAuthTokenRequest'
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthTokenResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /customer/oauth/revoke:
    post:
      operationId: revokeStorefrontCustomerToken
      summary: "Revoke customer token family"
      description: "Access veya refresh token ailesini güvenli biçimde iptal eder."
      tags: ["Customer and access"]
      x-shopiyz-auth: public
      x-shopiyz-cache: none
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OAuthRevokeRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/OAuthRevokeRequest'
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthRevokeResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /customer/me:
    get:
      operationId: getExternalStorefrontCustomer
      summary: "Get authorized customer profile"
      description: "customer:read kapsamıyla minimum müşteri profilini döndürür."
      tags: ["Customer and access"]
      x-shopiyz-auth: customer_oauth
      x-shopiyz-cache: private
      x-required-scopes: ["customer:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - CustomerOAuthBearer: []
    patch:
      operationId: updateExternalStorefrontCustomer
      summary: "Update authorized customer profile"
      description: "customer:write kapsamıyla izin verilen minimum profil alanlarını günceller."
      tags: ["Customer and access"]
      x-shopiyz-auth: customer_oauth
      x-shopiyz-cache: none
      x-required-scopes: ["customer:write"]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerProfilePatch'
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - CustomerOAuthBearer: []
  /newsletter-subscriptions:
    post:
      operationId: createNewsletterSubscription
      summary: "Request newsletter subscription"
      description: "Çift onaylı e-posta aboneliği başlatır; enumeration-safe 202 yanıtı, bot koruması ve consent kaydı kullanır."
      tags: ["Customer and access"]
      x-shopiyz-auth: public_or_storefront_token
      x-shopiyz-cache: none
      x-required-scopes: ["storefront:read"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NewsletterSubscriptionRequest'
      responses:
        '202':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NewsletterAcceptedResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - {}
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /password:
    post:
      operationId: verifyStorefrontPassword
      summary: "Verify storefront password"
      description: "Parola korumalı mağaza erişimini doğrular ve güvenli erişim çerezi oluşturur."
      tags: ["Customer and access"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /contact-us:
    post:
      operationId: submitStorefrontContact
      summary: "Submit contact form"
      description: "Tema iletişim formunu doğrular ve mağaza iletişim akışına iletir."
      tags: ["Customer and access"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /blog-comments:
    post:
      operationId: submitBlogComment
      summary: "Submit blog comment"
      description: "Blog yazısı yorumunu moderasyon kuyruğuna gönderir."
      tags: ["Customer and access"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /customer/social/{provider}/start:
    get:
      operationId: startStorefrontSocialLogin
      summary: "Start social login"
      description: "Yapılandırılmış sosyal giriş sağlayıcısı için state doğrulamalı OAuth yönlendirmesini başlatır."
      tags: ["Customer and access"]
      x-shopiyz-auth: public
      x-shopiyz-cache: none
      parameters:
        - name: provider
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /customer/social/{provider}/callback:
    get:
      operationId: completeStorefrontSocialLogin
      summary: "Complete social login"
      description: "Sağlayıcı callback'ini doğrular, müşteri oturumunu oluşturur ve güvenli dönüş adresine yönlendirir."
      tags: ["Customer and access"]
      x-shopiyz-auth: public
      x-shopiyz-cache: none
      parameters:
        - name: provider
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /customer/social/{provider}/revoke:
    get:
      operationId: getStorefrontSocialRevokeStatus
      summary: "Validate social revoke route"
      description: "Sosyal bağlantı kaldırma callback'i için sağlayıcı anahtarını doğrular."
      tags: ["Customer and access"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      parameters:
        - name: provider
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
    post:
      operationId: acknowledgeStorefrontSocialRevoke
      summary: "Acknowledge social revoke callback"
      description: "Sağlayıcı tarafından gönderilen bağlantı kaldırma callback'ini doğrular ve kabul eder."
      tags: ["Customer and access"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      parameters:
        - name: provider
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /wishlist-share:
    post:
      operationId: shareWishlist
      summary: "Share wishlist"
      description: "İstek listesi paylaşım bağlantısını veya e-posta teslimatını oluşturur."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /ask-expert:
    post:
      operationId: submitAskExpert
      summary: "Submit ask-an-expert request"
      description: "Ürün uzmanı talebini mağazanın yapılandırılmış destek kanalına iletir."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /stock-alerts:
    post:
      operationId: manageStockAlert
      summary: "Create or manage stock alert"
      description: "Stok bildirimi aboneliği oluşturur, doğrular veya iptal eder."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /cart-recovery-cart:
    get:
      operationId: getRecoveredCart
      summary: "Get recovered cart"
      description: "Geçerli kurtarma belirteci için sepet içeriğini döndürür."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: private
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
        - name: cartToken
          in: query
          required: false
          description: "Tek kullanımlık sepet kurtarma belirteci."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /cart-recovery-event:
    post:
      operationId: recordCartRecoveryEvent
      summary: "Record cart recovery event"
      description: "Sepet kurtarma akışı için anonim oturum olayını kaydeder."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /shopify-checkout:
    post:
      operationId: createExternalCheckout
      summary: "Create external checkout"
      description: "Yapılandırılmış harici ödeme sağlayıcısına güvenli checkout devri oluşturur."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /checkout-sessions:
    post:
      operationId: createHeadlessCheckoutSession
      summary: "Create headless checkout session"
      description: "Kanonik ürün, fiyat, stok, kampanya ve vergi doğrulamasıyla kısa ömürlü tek kullanımlık checkout URL'si oluşturur."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: storefront_token
      x-shopiyz-cache: none
      x-required-scopes: ["storefront:checkout"]
      parameters:
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string, minLength: 8, maxLength: 200 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckoutSessionRequest'
      responses:
        '201':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutSessionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontAccessToken: []
        - StorefrontBearerAuth: []
  /checkout-sessions/{token}:
    get:
      operationId: consumeHeadlessCheckoutSession
      summary: "Consume headless checkout session"
      description: "İmzalı checkout tokenini bir kez kullanır ve native checkout'a aktarılacak kanonik sepeti döndürür."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: public
      x-shopiyz-cache: none
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
        - name: storeId
          in: query
          required: false
          description: "Yalnız development/preview ortamında açık mağaza kimliği."
          schema: { type: string }
        - name: preview
          in: query
          required: false
          description: "Yetkili aynı-origin oturumunda taslak içerik ön izlemesi."
          schema: { type: boolean }
        - name: previewThemeId
          in: query
          required: false
          description: "Ön izlenecek kurulu tema kimliği."
          schema: { type: string }
        - name: designId
          in: query
          required: false
          description: "Ön izlenecek tema düzeni kimliği."
          schema: { type: string }
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutSessionConsumeResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security: []
  /reviews:
    post:
      operationId: submitProductReview
      summary: "Submit product review"
      description: "Ürün değerlendirmesini doğrulama ve moderasyon akışına gönderir."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
  /review-questions:
    post:
      operationId: submitProductQuestion
      summary: "Submit product question"
      description: "Ürün sorusunu doğrulama ve moderasyon akışına gönderir."
      tags: ["Commerce and engagement"]
      x-shopiyz-auth: same_origin
      x-shopiyz-cache: none
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Successful Storefront response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorefrontResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
      security:
        - StorefrontSession: []
components:
  securitySchemes:
    StorefrontAccessToken:
      type: apiKey
      in: header
      name: X-Shopiyz-Storefront-Access-Token
    StorefrontBearerAuth:
      type: http
      scheme: bearer
      bearerFormat: shpft
    StorefrontSession:
      type: apiKey
      in: cookie
      name: faprika_session
    CustomerOAuthBearer:
      type: http
      scheme: bearer
      bearerFormat: shpcat
  schemas:
    StorefrontResponse:
      type: object
      additionalProperties: true
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            details: {}
    CheckoutLineInput:
      type: object
      additionalProperties: false
      required: [variantId, quantity]
      properties:
        variantId: { type: string, minLength: 1 }
        quantity: { type: integer, minimum: 1, maximum: 99 }
    CheckoutSessionRequest:
      type: object
      additionalProperties: false
      required: [lines, returnUrl, cancelUrl]
      properties:
        lines: { type: array, minItems: 1, maxItems: 50, items: { $ref: '#/components/schemas/CheckoutLineInput' } }
        locale: { type: string }
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        returnUrl: { type: string, format: uri }
        cancelUrl: { type: string, format: uri }
    CheckoutSessionResponse:
      type: object
      required: [id, checkoutId, status, checkoutUrl, expiresAt, currency, lines, subtotal, discountTotal, shippingTotal, taxTotal, total, pricing]
      properties:
        id: { type: string }
        checkoutId: { type: string }
        status: { type: string, enum: [pending] }
        checkoutUrl: { type: string, format: uri }
        expiresAt: { type: string, format: date-time }
        locale: { type: string }
        currency: { type: string }
        lines: { type: array, items: { type: object, additionalProperties: true } }
        subtotal: { type: integer, minimum: 0, description: Minor currency units }
        discountTotal: { type: integer, minimum: 0, description: Minor currency units }
        shippingTotal: { type: [integer, 'null'], minimum: 0, description: Address is not known at session creation }
        taxTotal: { type: integer, minimum: 0, description: Minor currency units }
        total: { type: integer, minimum: 0, description: Minor currency units }
        pricing: { type: object, additionalProperties: true }
    CheckoutSessionConsumeResponse:
      allOf:
        - $ref: '#/components/schemas/CheckoutSessionResponse'
        - type: object
          properties:
            status: { type: string, enum: [consumed] }
            returnUrl: { type: string, format: uri }
            cancelUrl: { type: string, format: uri }
    OAuthTokenRequest:
      type: object
      required: [grant_type, client_id]
      properties:
        grant_type: { type: string, enum: [authorization_code, refresh_token] }
        client_id: { type: string }
        code: { type: string }
        code_verifier: { type: string, minLength: 43, maxLength: 128 }
        redirect_uri: { type: string, format: uri }
        refresh_token: { type: string }
    OAuthTokenResponse:
      type: object
      required: [token_type, access_token, expires_in, refresh_token, scope]
      properties:
        token_type: { type: string, enum: [Bearer] }
        access_token: { type: string }
        expires_in: { type: integer }
        refresh_token: { type: string }
        refresh_token_expires_in: { type: integer }
        scope: { type: string }
        nonce: { type: string }
    OAuthRevokeRequest:
      type: object
      required: [client_id, token]
      properties:
        client_id: { type: string }
        token: { type: string }
    OAuthRevokeResponse:
      type: object
      required: [revoked]
      properties: { revoked: { type: boolean } }
    CustomerProfilePatch:
      type: object
      additionalProperties: false
      properties:
        firstName: { type: [string, 'null'] }
        lastName: { type: [string, 'null'] }
        phone: { type: [string, 'null'] }
        locale: { type: string }
        acceptsMarketingEmail: { type: boolean }
    CustomerProfileResponse:
      type: object
      required: [customer]
      properties:
        customer: { type: object, additionalProperties: false }
    NewsletterSubscriptionRequest:
      type: object
      additionalProperties: false
      required: [email, consent]
      properties:
        email: { type: string, format: email }
        locale: { type: string }
        source: { type: string, maxLength: 120 }
        consent: { type: boolean, const: true }
        website: { type: string, description: Honeypot; gerçek kullanıcılar boş bırakır. }
        turnstileToken: { type: string }
    NewsletterAcceptedResponse:
      type: object
      required: [accepted, message]
      properties:
        accepted: { type: boolean, const: true }
        message: { type: string }
    StorefrontMeta:
      type: object
      properties:
        apiVersion: { type: string }
        storefrontRevision: { type: [string, 'null'] }
    Localization:
      type: object
      required: [currency, locale, currencySymbol, minimumFractionDigits, maximumFractionDigits, markets, availableCountries]
      properties:
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        locale: { type: string }
        currencySymbol: { type: string }
        minimumFractionDigits: { type: integer, minimum: 0 }
        maximumFractionDigits: { type: integer, minimum: 0 }
        timezone: { type: string }
        markets:
          type: array
          items:
            type: object
            required: [id, name, countries, currency]
            properties:
              id: { type: string }
              name: { type: string }
              countries: { type: array, items: { type: string, pattern: '^[A-Z]{2}$' } }
              currency: { type: string, pattern: '^[A-Z]{3}$' }
        availableCountries: { type: array, items: { type: string, pattern: '^[A-Z]{2}$' } }
    ProductVariant:
      type: object
      required: [id, productId, price, inventoryQuantity, availableForSale]
      properties:
        id: { type: string }
        productId: { type: string }
        title: { type: [string, 'null'] }
        sku: { type: [string, 'null'] }
        price: { type: number, minimum: 0 }
        compareAtPrice: { type: [number, 'null'], minimum: 0 }
        inventoryQuantity: { type: integer }
        availableForSale: { type: boolean }
        optionValues: { type: array, items: { type: string } }
        imageUrl: { type: [string, 'null'], format: uri-reference }
      description: Maliyet değeri Storefront yanıtında null olarak maskelenir; tedarikçi maliyet verisi yayınlanmaz.
    ProductImage:
      type: object
      required: [id, url, mediaType, position]
      properties:
        id: { type: string }
        url: { type: string, format: uri-reference }
        altText: { type: [string, 'null'] }
        mediaType: { type: string }
        position: { type: integer }
        width: { type: [integer, 'null'] }
        height: { type: [integer, 'null'] }
    Product:
      type: object
      required: [id, title, handle, status, tags, minPrice, maxPrice, inventoryQuantity]
      properties:
        id: { type: string }
        title: { type: string }
        handle: { type: string }
        status: { type: string, enum: [active] }
        vendor: { type: [string, 'null'] }
        productType: { type: [string, 'null'] }
        productCategory: { type: [string, 'null'] }
        description: { type: [string, 'null'] }
        tags: { type: array, items: { type: string } }
        featuredImageUrl: { type: [string, 'null'], format: uri-reference }
        availableForSale: { type: boolean }
        preorderOnly: { type: boolean }
        inventoryQuantity: { type: integer }
        minPrice: { type: number, minimum: 0 }
        maxPrice: { type: number, minimum: 0 }
        minCompareAtPrice: { type: [number, 'null'], minimum: 0 }
        optionNames: { type: array, items: { type: string } }
        variants: { type: array, items: { $ref: '#/components/schemas/ProductVariant' } }
        images: { type: array, items: { $ref: '#/components/schemas/ProductImage' } }
        metafields: { type: object, additionalProperties: true }
      additionalProperties: true
    Collection:
      type: object
      required: [id, title, slug, status, productCount]
      properties:
        id: { type: string }
        title: { type: string }
        slug: { type: string }
        status: { type: string, enum: [active] }
        description: { type: [string, 'null'] }
        imageUrl: { type: [string, 'null'], format: uri-reference }
        productCount: { type: integer, minimum: 0 }
        sortOrder: { type: string }
      additionalProperties: true
    FacetValue:
      type: object
      required: [value, count]
      properties:
        value: { type: string }
        count: { type: integer, minimum: 0 }
    CatalogFacets:
      type: object
      required: [vendors, productTypes, categories, tags, colors, sizes, availability, price]
      properties:
        vendors: { type: array, items: { $ref: '#/components/schemas/FacetValue' } }
        productTypes: { type: array, items: { $ref: '#/components/schemas/FacetValue' } }
        categories: { type: array, items: { $ref: '#/components/schemas/FacetValue' } }
        tags: { type: array, items: { $ref: '#/components/schemas/FacetValue' } }
        colors: { type: array, items: { $ref: '#/components/schemas/FacetValue' } }
        sizes: { type: array, items: { $ref: '#/components/schemas/FacetValue' } }
        availability: { type: array, items: { $ref: '#/components/schemas/FacetValue' } }
        price:
          type: object
          properties:
            minimum: { type: number, minimum: 0 }
            maximum: { type: number, minimum: 0 }
    Pagination:
      type: object
      required: [page, limit, total, unfilteredTotal, pageCount, hasPreviousPage, hasNextPage, capped]
      properties:
        page: { type: integer, minimum: 1 }
        limit: { type: integer, minimum: 1, maximum: 48 }
        total: { type: integer, minimum: 0 }
        unfilteredTotal: { type: integer, minimum: 0 }
        pageCount: { type: integer, minimum: 1 }
        hasPreviousPage: { type: boolean }
        hasNextPage: { type: boolean }
        capped: { type: boolean, description: 'true ise katalog güvenlik penceresi 250 üründe sınırlandı.' }
    ProductConnectionResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [products, facets, pagination, appliedFilters, sort, localization]
          properties:
            products: { type: array, items: { $ref: '#/components/schemas/Product' } }
            facets: { $ref: '#/components/schemas/CatalogFacets' }
            pagination: { $ref: '#/components/schemas/Pagination' }
            appliedFilters: { type: object, additionalProperties: true }
            sort: { type: string }
            localization: { $ref: '#/components/schemas/Localization' }
    ProductResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [product, localization]
          properties:
            product: { $ref: '#/components/schemas/Product' }
            localization: { $ref: '#/components/schemas/Localization' }
    CollectionListResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [collections]
          properties:
            collections: { type: array, items: { $ref: '#/components/schemas/Collection' } }
    CollectionResponse:
      allOf:
        - $ref: '#/components/schemas/ProductConnectionResponse'
        - type: object
          required: [collection]
          properties:
            collection: { $ref: '#/components/schemas/Collection' }
            filterSettings: { type: [object, 'null'], additionalProperties: true }
    MenuItem:
      type: object
      required: [id, title, itemType, url, openInNewTab, children]
      properties:
        id: { type: string }
        title: { type: string }
        itemType: { type: string }
        url: { type: string, format: uri-reference }
        openInNewTab: { type: boolean }
        children: { type: array, items: { $ref: '#/components/schemas/MenuItem' } }
    Menu:
      type: object
      required: [id, title, handle, location, items]
      properties:
        id: { type: string }
        title: { type: string }
        handle: { type: string }
        location: { type: string, enum: [main, footer, customer_accounts, custom] }
        items: { type: array, items: { $ref: '#/components/schemas/MenuItem' } }
    MenuListResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [menus]
          properties:
            menus: { type: array, items: { $ref: '#/components/schemas/Menu' } }
    Promotion:
      type: object
      required: [id, title, kind, valueType, appliesToType, productIds, collectionIds, startsAt]
      properties:
        id: { type: string }
        title: { type: string }
        kind: { type: string, enum: [amountOffProducts, amountOffOrder, buyXGetY, freeShipping] }
        valueType: { type: string, enum: [percentage, fixed, free] }
        valueAmount: { type: [number, 'null'] }
        maximumDiscountAmount: { type: [number, 'null'] }
        appliesToType: { type: string, enum: [all, products, collections] }
        productIds: { type: array, items: { type: string } }
        collectionIds: { type: array, items: { type: string } }
        minimumRequirementType: { type: string, enum: [none, subtotal, quantity] }
        minimumSubtotal: { type: [number, 'null'] }
        minimumQuantity: { type: [integer, 'null'] }
        startsAt: { type: string, format: date-time }
        endsAt: { type: [string, 'null'], format: date-time }
        config: { type: object, additionalProperties: true }
      description: Yalnız herkese açık otomatik kampanyalar döner; indirim kodu alanı bulunmaz.
    PromotionListResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [promotions, localization]
          properties:
            promotions: { type: array, items: { $ref: '#/components/schemas/Promotion' } }
            localization: { $ref: '#/components/schemas/Localization' }
    LocalizationResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [localization]
          properties:
            localization: { $ref: '#/components/schemas/Localization' }
    ShopResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [shop, settings, localization, promotions]
          properties:
            shop: { type: object, additionalProperties: true }
            settings: { type: object, additionalProperties: true }
            activeTheme: { type: [object, 'null'], additionalProperties: true }
            headerLayout: { type: [object, 'null'], additionalProperties: true }
            footerLayout: { type: [object, 'null'], additionalProperties: true }
            localization: { $ref: '#/components/schemas/Localization' }
            promotions: { type: array, items: { $ref: '#/components/schemas/Promotion' } }
    StorefrontPageResponse:
      type: object
      required: [store, settings, products, collections, menus, localization, promotions, storefrontRevision]
      properties:
        store: { type: object, additionalProperties: true }
        settings: { type: object, additionalProperties: true }
        activeTheme: { type: [object, 'null'], additionalProperties: true }
        themeLayout: { type: [object, 'null'], additionalProperties: true }
        headerLayout: { type: [object, 'null'], additionalProperties: true }
        footerLayout: { type: [object, 'null'], additionalProperties: true }
        products: { type: array, items: { $ref: '#/components/schemas/Product' } }
        collections: { type: array, items: { $ref: '#/components/schemas/Collection' } }
        menus: { type: array, items: { $ref: '#/components/schemas/Menu' } }
        selectedProduct: { oneOf: [{ $ref: '#/components/schemas/Product' }, { type: 'null' }] }
        selectedCollection: { oneOf: [{ $ref: '#/components/schemas/Collection' }, { type: 'null' }] }
        localization: { $ref: '#/components/schemas/Localization' }
        promotions: { type: array, items: { $ref: '#/components/schemas/Promotion' } }
        storefrontRevision: { type: string }
      additionalProperties: true
    ContentPageResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [page]
          properties:
            page: { type: object, additionalProperties: true }
    BlogListResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [blogs]
          properties:
            blogs: { type: array, items: { type: object, additionalProperties: true } }
    BlogResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [blog, posts]
          properties:
            blog: { type: object, additionalProperties: true }
            posts: { type: array, items: { type: object, additionalProperties: true } }
    BlogPostResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [blog, post, products]
          properties:
            blog: { type: object, additionalProperties: true }
            post: { type: object, additionalProperties: true }
            products: { type: array, items: { $ref: '#/components/schemas/Product' } }
    SearchResponse:
      allOf:
        - $ref: '#/components/schemas/StorefrontMeta'
        - type: object
          required: [search, products, collections]
          properties:
            search: { type: [object, 'null'], additionalProperties: true }
            products: { type: array, items: { $ref: '#/components/schemas/Product' } }
            collections: { type: array, items: { $ref: '#/components/schemas/Collection' } }
    StorefrontError:
      type: object
      required: [error]
      properties:
        error: { type: string }
  responses:
    BadRequest:
      description: HTTP 400
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: HTTP 401
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: HTTP 403
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: HTTP 404
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: HTTP 409
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
    UnprocessableEntity:
      description: HTTP 422
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
    RateLimited:
      description: HTTP 429
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
    InternalError:
      description: HTTP 500
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/StorefrontError'
              - $ref: '#/components/schemas/ErrorResponse'
